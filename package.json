{
  "name": "awaaz-rajasthan-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "postinstall": "npm --prefix backend install --omit=dev && npm --prefix frontend install"
  }
}
